# Cursor / experiment log

Generated by Cursor. 2026-08-18. Newest entries at the top. Commit and push this file with shared source.

## 2026-09-09 — Record T1-scan and Wave A analysis commit hash

- Commit: `1608e08fb`
- What: Vertex T1-threshold drivers (`-a`, compact `-c`), Wave A \(\tau_\alpha\) plots, static-statistics comparison, and login-node Cursor rule.

## 2026-09-09 — Vertex vs Voronoi static statistics note

- Commit: `1608e08fb`
- What: At the same \((p_0,T)\) the vertex perimeter PDF is Gaussian, but \(\langle P\rangle(T)\) and \(\langle q\rangle(T)\) sit above Voronoi and rise faster. Combined \(2\times 3\) \(p_0\) panels for both means (both models) and for vertex \(P(\hat P)\).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_stat_compare_panels.py` → `vertexModel_statDist/mean_P_vs_T_all_p0.png`, `mean_q_vs_T_all_p0.png`, `vertex_P_gaussian_all_p0.png`. Note: [`vertexModel_statCompare_report.md`](glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_statCompare_report.md).

## 2026-09-09 — T1-scan experiment note

- What: Markdown of the vertex T1-threshold campaign and the \(\ell_{T1}\) effect on \(\tau_\alpha\). Usable window \(\sim 10^{-3}\)–\(0.03\): smaller \(\ell_{T1}\) slows CRSISF \(1/e\); \(\tau_\alpha\ell_{T1}\) vs \(T\) nearly collapses. \(\ell_{T1}=10^{-4}\) never crosses \(1/e\); \(\ell_{T1}\gtrsim 0.04\) is noisy / crash-prone. Default \(0.01\) is not a plateau.
- How: [`glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_t1Scan_report.md`](glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_t1Scan_report.md).

## 2026-09-09 — Wave A figures: archive \(p_0=3.800\) \(\tau_\alpha\), no cut in titles

- Commit: `1608e08fb`
- What: Combined 2×3 plots now use the earlier \(p_0=3.800\) t1Scan \(\tau_\alpha\) (`vertex_t1_tau_vs_T.csv`, `source=t1Scan`) instead of the leftover last-\(t_w\) harvest. Other \(p_0\) still from Wave A CRSISF. Combined x-labels only on the bottom row; titles are \(N=4096\) / \(p_0\) only (no \(5\tau_{\mathrm{est}}\) wording).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_waveA_panels.py` and `--nocut`. Overwrites `vertex_t1_tau_*_all_p0*.png` and `*_p3.xxx*.png`.

## 2026-09-09 — Wave A panels without \(5\tau_{\mathrm{est}}\) cut

- Commit: `788ffc815`
- What: Same t1Scan CRSISF \(1/e\) \(\tau_\alpha\), but keep every crossing (no \(\tau_\alpha\le 5\tau_{\mathrm{est}}\)). 517/700 kept (was 427 with the cut). Original cut figures left in place.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_waveA_panels.py --nocut` → `vertex_t1_tau_waveA_nocut.csv`, `vertex_t1_tau_vs_lth_all_p0_nocut.png`, `vertex_t1_tau_vs_T_all_p0_nocut.png`, `vertex_t1_tau_lth_vs_T_all_p0_nocut.png`, and `*_p3.xxx_nocut.png`.

## 2026-09-09 — Wave A CRSISF and \(p_0\)-panel \(\tau_\alpha\) plots

- Commit: `1608e08fb`
- What: 550 Wave A FS jobs COMPLETED. \(\tau_\alpha\) = longest-\(t_w\) CRSISF \(1/e\) at \(q=6.80\); drop if no crossing or \(\tau_\alpha>5\tau_{\mathrm{est}}\). 700 t1Scan points, **427 kept**. Combined 2×3 figures plus per-\(p_0\) \(\tau_\alpha(\ell_{T1})\), \(\tau_\alpha(T)\), \(\tau_\alpha\ell_{T1}(T)\).
- How: `local/submit/submitVertexTauAlphaFsT1AllP0.sh` → SLURM `21922185`–`21922742`. `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_waveA_panels.py` → `vertexModel_t1Scan/vertex_t1_tau_waveA.csv`, `vertex_t1_tau_vs_lth_all_p0.png`, `vertex_t1_tau_vs_T_all_p0.png`, `vertex_t1_tau_lth_vs_T_all_p0.png`, and `*_p3.xxx.png`.

## 2026-09-09 — Vertex scratch cleanup (keep Voronoi)

- Commit: `1608e08fb`
- What: Deleted superseded default-\(\ell_{T1}=0.01\) raw files (Batch 1 Voronoi \(\tau_{\mathrm{est}}\); Batch 2 \(T\) retargeted in Batch 3; Batch 4 `T_old`) and extra T1-scan `glassyDynamics` / CRSISF, keeping the last valid `waitingTime` per \((\ell_{T1},p_0,T)\). 15034 files, 194.71 GiB. Fluids and Voronoi `productionRuns/` untouched. Scratch went from 100% full to ~91% (~94G free). T1 production now writes only \(k=14\).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/cleanup_vertex_storage.py --dry-run` then `--execute`. Driver: `vertexTauAlphaTest.cpp` T1 scan still evolves 15 \(\tau_{\mathrm{block}}\) but registers only \(k=14\). Other clones: `git pull` then rebuild `vertexTauAlphaTest.out` before the next T1 wave.

## 2026-09-09 — Wave A compact save: 10\(\tau\) eq + 5\(\tau\) record

- Commit: `1608e08fb`
- What: \(\tau=\max(\tau_{\mathrm{est}},1000)\). Equilibrate \(10\tau\) with no trajectory dumps, then one log-spaced trajectory for \(5\tau\) at \(t_w=10\tau\). Deleted 55 failed Wave A dirs (`p3.750`…`p3.850`; kept `p3.800`). Resubmitted 550 compact jobs.
- How: `vertexTauAlphaTestCompact.out -c`. `local/submit/submitVertexTauAlphaT1AllP0.sh` → SLURM `21912363`–`21912913` (550 jobs; some IDs unused). After production: `submitVertexTauAlphaFsT1AllP0.sh`.

## 2026-09-09 — Wave A all-\(p_0\) T1 scan: all 550 FAILED (disk quota)

- Commit: `788ffc815`
- What: No \(\tau_\alpha\) from Wave A. Fluids loaded, \(q\) finite, equilibration finished; every job died at the first production write with `Disk quota exceeded` (exit 3:0, ~22–26 min). `/work/hdd/bbtm` (= `/scratch/bbtm/cli6`) is over quota (1.075T used / 1000G soft / 1.074T hard). Leftover `t1_*/p3.750`…`p3.850` trees are empty NetCDF stubs plus a few ~30 MB `waitingTime=0` files; no CRSISF. Keep existing \(p_0=3.800\) t1Scan. Another production round is required after freeing \(\gtrsim 300\) GB (complete Wave A \(\approx 11\times5\times5\) GB).
- How: `sacct --name vertexT1ap` (550 FAILED). Do not resubmit until quota is cleared; then `submitVertexTauAlphaT1AllP0.sh` again. Do not submit Wave B or FS yet.

## 2026-09-08 — Vertex T1-threshold \(\tau_\alpha\) scan for all \(p_0\)

- Commit: `1608e08fb`
- What: Same T1-threshold protocol as \(p_0=3.800\) for the other five packings (\(3.750, 3.775, 3.815, 3.825, 3.850\)). Skip \(p_0=3.800\) and \(\ell_{T1}=0.01\). \(\ell_{T1}=0.001\)–\(0.1\) (15 values, not \(10^{-4}\)). \(N=4096\), `idx=0`, \(\tau_{\mathrm{block}}=\max(\tau_{\mathrm{est}},1000)\), ten recommended \(T\) per \(p_0\). Wave A: \(\ell_{T1}\le 0.06\) (550 CPU jobs, ~1040 CPU-h). Wave B: \(0.07\)–\(0.1\) (200 jobs, ~370 CPU-h extra; crash-prone at 3.8). A+B ≈ 1410 CPU-h (~1 SU/core-h on Delta `cpu`).
- How: CSV `glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_t1Scan/state_points_all_p0.csv` and `state_points_all_p0_waveB.csv`. Wave A production: `local/submit/submitVertexTauAlphaT1AllP0.sh` → SLURM `21890572`–`21891127` (550 jobs; some IDs unused). After production: `submitVertexTauAlphaFsT1AllP0.sh`. Wave B not submitted. Plots: `plot_vertex_t1_tau_alpha.py` and `plot_vertex_t1_tau_vs_lth.py` loop all six \(p_0\).

## 2026-09-08 — Vertex \(\tau_\alpha \ell_{T1}\) vs \(T\)

- Commit: `788ffc815`
- What: Trial combination \(\tau_\alpha \ell_{T1}\) vs \(T\) at \(p_0=3.800\). Color is \(\ell_{T1}\). t1Scan grid only.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_lth_vs_T.py` → `vertexModel_t1Scan/vertex_t1_tau_lth_vs_T.png`.

## 2026-09-08 — Vertex \(\tau_\alpha\) vs \(\ell_{T1}/T^{1/2}\)

- Commit: `788ffc815`
- What: Longest-crossed CRSISF \(1/e\) \(\tau_\alpha\) vs reduced threshold \(\ell_{T1}/T^{1/2}\) at \(p_0=3.800\). Color is \(\ell_{T1}\). t1Scan grid only.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_vs_lth_over_sqrtT.py` → `vertexModel_t1Scan/vertex_t1_tau_vs_lth_over_sqrtT.png`.

## 2026-09-08 — Vertex \(\tau_\alpha T^{2}\) vs \(\ell_{T1}\)

- Commit: `788ffc815`
- What: Trial combination \(\tau_\alpha T^{2}\) vs \(\ell_{T1}\) at \(p_0=3.800\). Color \(\log T\), red hot to blue cold. t1Scan grid only.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_T2_vs_lth.py` → `vertexModel_t1Scan/vertex_t1_tau_T2_vs_lth.png`.

## 2026-09-08 — Vertex \(\tau_\alpha T^{1/2}\) vs \(\ell_{T1}\)

- Commit: `788ffc815`
- What: Trial \(T\)-reduced relaxation time \(\tau_\alpha T^{1/2}\) vs \(\ell_{T1}\) at \(p_0=3.800\). Color \(\log T\), red hot to blue cold. t1Scan grid only.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_sqrtT_vs_lth.py` → `vertexModel_t1Scan/vertex_t1_tau_sqrtT_vs_lth.png`.

## 2026-09-08 — Vertex τ_α vs ℓ_T1 T^{1/2}, color by ℓ_T1

- Commit: `788ffc815`
- What: Longest-crossed CRSISF \(1/e\) \(\tau_\alpha\) vs \(\ell_{T1}T^{1/2}\) at \(p_0=3.800\) (trial reduced threshold if edge fluctuations scale as \(T^{1/2}\)). Color is \(\ell_{T1}\). t1Scan grid only.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_vs_lth_sqrtT.py` → `vertexModel_t1Scan/vertex_t1_tau_vs_lth_sqrtT.png`.

## 2026-09-08 — Vertex τ_α vs ℓ_T1, color by T

- Commit: `788ffc815`
- What: Longest-crossed CRSISF \(1/e\) \(\tau_\alpha\) vs \(\ell_{T1}\) at \(p_0=3.800\). One polyline per recommended \(T\); color \(\log T\), red hot to blue cold. t1Scan grid only (not the default-0.01 scouting \(T\)).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_vs_lth.py` reads `vertex_t1_tau_vs_T.csv` → `vertexModel_t1Scan/vertex_t1_tau_vs_lth.png`.

## 2026-09-08 — Vertex vs Voronoi standardized A, P and P(ℓ)

- Commit: `788ffc815` (pre-change HEAD)
- What: Last 8 log-spaced frames from longest-valid trajectories (q-map inventory). Standardized \(\hat A\), \(\hat P\) vs \(\mathcal N(0,1)\) and raw \(P(\ell)\). One figure per \(p_0\); vertex solid, Voronoi dashed; color \(=T\). No \((p_0,T)\) matching. 185/200 dumps succeeded; 15 Voronoi `waitingTime999999` files failed NetCDF (same schema set as the q-map harvest).
- How: `stateCompareDump.out -k all -d 8`. `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_statDist/plot_stat_dist.py`. Outputs: `stat_dist_p3.750.png` … `p3.850.png`, `moments.csv`. Other clones need `git pull` then rebuild `stateCompareDump.out` for `-d`.

## 2026-09-08 — Vertex τ_α vs T including ℓ_T1 = 0.04–0.09

- Commit: `788ffc815` (FS submit HEAD)
- What: Regenerated longest-crossed CRSISF \(1/e\) \(\tau_\alpha(T)\) at \(p_0=3.800\) with \(\ell_{T1}=0.04\)–\(0.09\) (\(\tau_{\mathrm{block}}=1000\)). \(0.04\): 10 \(T\) crossed. \(0.05\)–\(0.06\): 6 \(T\) crossed (full CRSISF). \(0.07\)–\(0.09\): partial FS (12 production and 12 FS jobs FAILED, segfault/heap). Overlay older \(\ell_{T1}\) and Voronoi.
- How: FS `local/submit/submitVertexTauAlphaFsT1004_009.sh` → SLURM `21885770`–`21885829`. `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_alpha.py` → `vertexModel_t1Scan/vertex_t1_tau_vs_T.png` and `.csv`.

## 2026-09-07 — Same-p0 ⟨q⟩ match, vertex vs Voronoi τ_α

- Commit: `788ffc815` (pre-change HEAD)
- What: At fixed \(p_0\), invert vertex \(T\) so \(\langle q\rangle_v(p_0,T_v)=\langle q\rangle_V(p_0,T_V)\). Overlap window: Voronoi \(\langle q\rangle\) inside the vertex \(\langle q\rangle(T)\) range (cold Voronoi, \(\langle q\rangle\) too close to \(p_0\), cannot match). 35 pairs with both \(\tau_\alpha\). Ratio \(\tau_v/\tau_V\) is \(120\)–\(3.3\times10^3\) (median \(\sim 310\)); similar \(\langle q\rangle\) at the same \(p_0\) does not match \(\tau_\alpha\). Contrast: previous invert shifted \(p_0\) at fixed \(T\).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_qMap/plot_q_match_same_p0.py` (reads existing q-map and \(\tau_\alpha\) CSVs; no new dumps). Outputs: `q_match_at_same_p0.csv`, `tau_vs_q_same_p0.png`, `tau_at_matched_q_same_p0.png`, `tau_ratio_same_p0.png`.

## 2026-09-07 — Vertex ℓ_T1 = 0.04–0.09, τ_est = 1000

- Commit: `788ffc815` (submit HEAD)
- What: Same ten recommended \(T\) at \(p_0=3.800\), \(N=4096\). \(\ell_{T1}=0.04,0.05,0.06,0.07,0.08,0.09\) (60 CPU jobs). Every job uses \(-t\,1000\) so \(\tau_{\mathrm{block}}=1000\) (not the long cold-\(T\) protocol). Folders `t1_0.04` … `t1_0.09`.
- How: CSV `glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_t1Scan/state_points_004_009.csv`. `local/submit/submitVertexTauAlphaT1004_009.sh` → SLURM `21865647`–`21865711` (60 jobs; some IDs unused). After production: `submitVertexTauAlphaFsT1004_009.sh` then the existing `plot_vertex_t1_tau_alpha.py`.

## 2026-09-07 — Vertex τ_α vs T for all ℓ_T1 (interior scan included)

- Commit: `788ffc815` (FS submit HEAD)
- What: Longest-crossed CRSISF \(1/e\) \(\tau_\alpha(T)\) at \(p_0=3.800\), \(N=4096\). New \(\ell_{T1}=0.0015,0.0022,0.0032,0.0046,0.0068,0.02,0.03\) all 150 CRSISF files each. Overlay default \(10^{-2}\), \(10^{-3}\), \(10^{-4}\) (no \(1/e\)), sparse \(10^{-1}\), and Voronoi.
- How: FS `local/submit/submitVertexTauAlphaFsT1Interior.sh` → SLURM `21865369`–`21865447` (70 jobs). `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_alpha.py` → `vertexModel_t1Scan/vertex_t1_tau_vs_T.png` and `.csv`.

## 2026-09-02 — Vertex vs Voronoi ⟨q⟩(p₀, T) harvest (no new jobs)

- Commit: `788ffc815` (pre-change HEAD)
- What: Mean shape \(q_i=P_i/\sqrt{A_i}\) from longest-waiting trajectories (vertex batches 2–4, Voronoi production `idx=10`). Vertex: 100/100 dumps. Voronoi: 85/100 (15 NetCDF schema failures, mostly `waitingTime999999`). At overlap \(T\), invert vertex \(p_0\) so \(\langle q\rangle_v=\langle q\rangle_V\): 11 matches, all with vertex \(p_0\) *below* the Voronoi \(p_0\). Interpolated \(\tau_\alpha^v/\tau_\alpha^V\) is still \(O(10^2)\)–\(O(10^3)\) (matching \(\langle q\rangle\) at the same \(T\) does not match \(\tau_\alpha\)).
- How: `stateCompareDump.out -k frames`. `python3 .../vertexModel_qMap/inventory_q_map.py` then `bash .../dump_q_map.sh` then `python3 .../plot_q_map.py`. Outputs: `vertex_q_vs_T.csv`, `voronoi_q_vs_T.csv`, `q_match_at_overlap_T.csv`, `q_vs_T_vertex_voronoi.png`, `tau_at_matched_q.png`. Rebuild dump target after `git pull` on other clones.

## 2026-09-02 — Extra vertex ℓ_T1 at p₀=3.8 (0.0015–0.0068, 0.02, 0.03)

- Commit: `788ffc815` (submit HEAD; tag-fix driver not committed yet)
- What: Same ten recommended \(T\) (\(\tau_k=10\)–\(10^4\)) as the first T1 scan. New \(\ell_{T1}=0.0015,0.0022,0.0032,0.0046,0.0068,0.02,0.03\) (70 CPU jobs). Do not rerun \(10^{-3}\) or \(10^{-2}\). Folder tag: exact decade stays `1e-N`; otherwise `%g` (`t1_0.02`, not `t1_1e-2`).
- How: CSV `glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_t1Scan/state_points_interior.csv`. `local/submit/submitVertexTauAlphaT1Interior.sh` → SLURM `21759717`–`21759788` (70 jobs; some IDs unused). After production: `submitVertexTauAlphaFsT1Interior.sh`. Smoke: `CELLGPU_SCRATCH=/tmp/cellgpu_vertex_t1_interior_smoke`, `-a 0.02` wrote `t1_0.02`.

## 2026-09-02 — Voronoi vs vertex static ensemble at matched (p0, T)

- Commit: `788ffc815` (pre-change HEAD)
- What: At \(p_0=3.800\), \(N=4096\), matched \(T=0.016,0.010,0.008\) (not matched \(\tau_\alpha\)). Vertex \(\langle q\rangle\) is \(0.023\)–\(0.039\) above Voronoi; \(p_5+p_7\) is lower and \(p_6\) higher (more hexatic). Vertex \(P(\ell)\) has no weight below \(\ell_{T1}=0.01\); Voronoi has a small tail. Verdict: not the same mechanical/topological state; \(\tau_\alpha\) at these \((p_0,T)\) is not like-for-like.
- How: `stateCompareDump.cpp` (CPU geometry; new `build-state-compare`, binary `glassyDynamicsProject/stateCompareDump.out`). `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_stateCompare/plot_state_compare.py`. Data `$CELLGPU_SCRATCH/N4096/vertex/p3.800/` idx=0 and `productionRuns/p3.800/` idx=10 (skip empty \(t_w=10^5\)). Overlay \(\ell_{T1}=10^{-3},10^{-4}\) at nearest \(T=0.00804331\) (not identical). Skip \(\ell_{T1}=10^{-1}\) (dump abort). Outputs: `means_vs_T.csv`, `state_compare_p3.800.png`. Other clones need the dump source then a host CMake target in a new build dir.

## 2026-09-02 — Vertex T1-threshold τ_α vs T

- Commit: `788ffc815` (FS submit HEAD)
- What: Longest-crossed CRSISF \(1/e\) \(\tau_\alpha(T)\) at \(p_0=3.800\), \(N=4096\), \(q=6.80\). \(\ell_{T1}=10^{-3}\) is slower than default \(10^{-2}\) at the same \(T\). \(\ell_{T1}=10^{-4}\): 150 CRSISF files, none crossed \(1/e\). \(\ell_{T1}=10^{-1}\): all 10 FS jobs aborted (heap / NetCDF), 7 files only.
- How: FS `local/submit/submitVertexTauAlphaFsT1.sh` → SLURM `21744559`–`21744593` (30 jobs; first 20 COMPLETED). Plot: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_t1_tau_alpha.py` → `vertexModel_t1Scan/vertex_t1_tau_vs_T.png` and `.csv`. Overlay Voronoi SISF \(1/e\) at \(p_0=3.800\) from `tauAlpha_2DVoronoi_all.csv` (open black squares).

## 2026-09-01 — Delta login-node Cursor worker rule

- What: Always-on repo rule so agents do not start or leave `cursor-agent` workers (or other long jobs) on NCSA Delta login nodes. Those hosts cap non-batch processes at 30 min CPU and 7 days wall clock; a worker started 2026-08-25 on `dt-login03` was killed 2026-09-01.
- How: `.cursor/rules/delta-login-nodes.mdc`; pointer in `multi-machine.mdc`. Does not stop Cursor IDE from spawning its own remote worker on SSH connect — disconnect when done, and rotate any API key that appeared in NCSA’s kill email.

## 2026-08-31 — Vertex T1-threshold τ_α scan at p₀=3.8

- Commit: `788ffc815` (submit HEAD; driver `-a` not committed yet)
- What: Scan whether \(\ell_{T1}\) (edge length for a T1; mean cell area 1; constructor default 0.01) changes vertex \(\tau_\alpha\). \(p_0=3.800\), \(N=4096\), \(dt=0.01\), `idx=0`, \(\tau_{\mathrm{block}}=\max(\tau_k,1000)\). Ten recommended \(T\) with \(\tau_k=10^{1+k/3}\) from 10 to \(10^4\). New \(\ell_{T1}=10^{-4},10^{-3},10^{-1}\) (30 CPU jobs). Skip \(10^{-2}\); reuse existing longest-crossed \(p_0=3.8\) as the default-threshold curve. Writes `$CELLGPU_SCRATCH/N4096/vertex/t1Scan/t1_<tag>/p3.800/` (does not touch `p3.800/`).
- How: `-a` on `vertexTauAlphaTest.out` / `vertexTauAlphaFs.out`. Rebuild `build-vertex-cuda129b`. CSV: `glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_t1Scan/state_points.csv`. `local/submit/submitVertexTauAlphaT1.sh` → SLURM `21655516`–`21655548` (30 jobs; some IDs unused). After production: `submitVertexTauAlphaFsT1.sh`. Smoke: `CELLGPU_SCRATCH=/tmp/cellgpu_vertex_t1_smoke`, fluid `-n 64 -i 200 -t 200`, tau `-a 0.0001 -t 1`. Other clones need the source then a rebuild before new jobs.

## 2026-08-31 — Vertex SAC and mean σ_xy from τ_α scouting

- Commit: `788ffc815` (pre-change HEAD)
- What: Stress autocorrelation \(C_\sigma(\Delta t)/T\) after \(10\tau_{\mathrm{block}}\) for all batches 2–4 longest-crossed \((p_0,T)\) (100 files). Grid: one panel per \(p_0\), log–log, \(T\) hot red to cold blue (non-positive \(C_\sigma\) omitted). Time average of \(\sigma_{xy}\) for \(t\ge 10\tau_{\mathrm{block}}\) vs \(T\), \(p_0\) red (3.750) to blue (3.850). Residual mean sits near 0.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_sac_and_sigma.py`. Data `$CELLGPU_SCRATCH/N4096/vertex/p*/`. Outputs in `vertexModel_tauAlpha_final/`: `vertex_SAC_vs_t_by_p0.png`, `vertex_sigmaXY_mean_vs_T.png`, `vertex_sigmaXY_mean_vs_T.csv`.

## 2026-08-26 — Vertex τ_α scouting report and Voronoi comparison

- Commit: `5c6ad88bb`
- What: Combined batches 2–4 longest-crossed CRSISF \(1/e\) \(\tau_\alpha\). Recommended production \(T\) at \(\tau_k=10^{1+k/3}\) by interpolating \(\log\tau\) vs \(1/T\). Two-panel figure: \(\tau_\alpha\) vs \(T\) and Angell \(\tau_\alpha\) vs \(T_g/T\) (\(T_g\): \(\tau_\alpha=10^4\)), vertex vs Voronoi.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_voronoi_comparison.py`. Report: `glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_tauAlpha_report.md`. Outputs in `vertexModel_tauAlpha_final/`.

## 2026-08-26 — Vertex τ_α batch 4 CRSISF vs target

- Commit: `2ee03b998` (submit HEAD)
- What: Production for the 12 retargeted \(T\) all COMPLETED. CRSISF \(q=6.80\); per-curve \(\tau_\alpha\) is the \(1/e\) crossing. Plots vs \(T\) per \(p_0\): red short \(t_w\) to blue long \(t_w\), black = batch-4 target. 180 curves, 168 crossed \(1/e\).
- How: Tau jobs `21471973`–`21471984`. FS: `local/submit/submitVertexTauAlphaFsBatch4.sh` → `21481736`–`21481747`. Then `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha_batch4.py` → `vertexModel_tauAlpha_batch4/vertexModel_tauAlpha_batch4_vs_T_p*.png`.

## 2026-08-26 — Vertex τ_α fourth-batch T retarget

- Commit: `2ee03b998` (submit HEAD)
- What: Longest-crossed \(t_w\) \(1/e\) \(\tau_\alpha\) from batches 2–3 as the measured curve. Rerun targets still off by \(\ge 1.5\times\) (12 jobs). New \(T\) from walking \(\log\tau_{\mathrm{meas}}\) vs \(1/T\).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/propose_vertex_tau_alpha_batch4.py`. `local/submit/submitVertexTauAlphaBatch4.sh` → SLURM `21471973`–`21471984`. After production: `submitVertexTauAlphaFsBatch4.sh` then `measure_and_plot_tau_alpha_batch4.py`.

## 2026-08-26 — Vertex τ_α batch 3 CRSISF vs target

- Commit: `2ee03b998` (submit HEAD)
- What: Production for the 28 retargeted \(T\) all COMPLETED. CRSISF \(q=6.80\); per-curve \(\tau_\alpha\) is the \(1/e\) crossing. Plots vs \(T\) per \(p_0\): red short \(t_w\) to blue long \(t_w\), black = batch-3 target. 420 curves, 390 crossed \(1/e\).
- How: Tau jobs `21451592`–`21451598`, `21451600`–`21451620`. FS: `local/submit/submitVertexTauAlphaFsBatch3.sh` → `21471751`–`21471778`. Then `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha_batch3.py` → `vertexModel_tauAlpha_batch3/vertexModel_tauAlpha_batch3_vs_T_p*.png`.

## 2026-08-25 — Vertex τ_α third-batch T retarget

- Commit: `2ee03b998` (submit HEAD)
- What: Longest-\(t_w\) batch-2 \(1/e\) \(\tau_\alpha\) as the measured curve. Rerun targets \(\ge 1000\) and hot points off by \(\ge 1.5\times\) (28 jobs). New \(T\) from \(\log\tau_{\mathrm{meas}}\) vs \(1/T\).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/propose_vertex_tau_alpha_batch3.py`. `local/submit/submitVertexTauAlphaBatch3.sh` → SLURM `21451592`–`21451598`, `21451600`–`21451620`.

## 2026-08-25 — Vertex τ_α batch 2 CRSISF vs target

- Commit: `2ee03b998` (submit HEAD)
- What: Second-batch production (60 jobs) all COMPLETED. CRSISF \(q=6.80\) on those trajectories; per-curve \(\tau_\alpha\) is the \(1/e\) crossing. Plots vs \(T\) per \(p_0\): red short \(t_w\) to blue long \(t_w\), black circles = batch-2 target \(\tau_{\alpha,k}=10^{1+k/3}\). 900 curves, 803 crossed \(1/e\).
- How: Tau jobs `21423560`–`21423619`. FS: `local/submit/submitVertexTauAlphaFsBatch2.sh` → `21450794`–`21450853`. Then `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha_batch2.py` → `vertexModel_tauAlpha_batch2/vertexModel_tauAlpha_batch2_vs_T_p*.png`.

## 2026-08-24 — Vertex τ_α second-batch T grid

- Commit: `2ee03b998` (submit HEAD; analysis uncommitted)
- What: Longest-\(t_w\) CRSISF \(1/e\) crossings give \(\tau_\alpha^{\mathrm{vertex}}\approx C(p_0)\tau_\alpha^{\mathrm{Voronoi}}\). Geometric-mean \(C\): 22.3, 28.6, 25.7, 47.4, 29.5, 42.8 for \(p_0=3.750\)–\(3.850\). Invented 10 \(T\) per \(p_0\) with \(\tau_{\alpha,k}=10^{1+k/3}\) (10 to \(10^4\)). Invert: \(\log\tau\) vs \(1/T\) in the Voronoi table; \(\log\tau\) vs \(\log T\) hotter than the table.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/propose_vertex_tau_alpha_batch2.py`. Points in `vertexModel_tauAlpha_batch2/`. `local/submit/submitVertexTauAlphaBatch2.sh` → SLURM `21423560`–`21423619` (60 CPU jobs, same `vertexTauAlphaTest.out`, existing \(T=0.1\) fluids). `-t` is the vertex \(\tau_\alpha\) target so \(\tau_{\mathrm{block}}=\max(\tau_{\mathrm{est}},1000)\).

## 2026-08-24 — Vertex CRSISF and per-curve τ_α

- Commit: uncommitted
- What: Cage-relative SISF of AVM cell centroids from first-batch vertex trajectories. Per-curve τ_α is the 1/e crossing (linear interpolation in Δt between the two samples that straddle 1/e). Plots: τ_α vs T per p0, red short t_w to blue long t_w, Voronoi estimate in black.
- How: Rebuild `vertexTauAlphaFs.out` (`build-vertex-cuda129b`). Default \(q=6.80\) (Voronoi `tauAlphaTableAnalyse`). FS on `$CELLGPU_SCRATCH/N4096/vertex/tauAlphaData/` (`k6.800` in the filename). First batch: SLURM `21413903`–`21413959` at \(q=2\pi\). Recompute: `21423359`–`21423415`. Plots: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha.py` and `plot_fs_grid_p3.800.py` → `vertexModel_tauAlpha_test/` (uses \(q=6.80\) files only).

## 2026-08-24 — AVM restart winding / NetCDF int read

- Commit: uncommitted
- What: Restart load of vertex fluids was producing NaN \(q\) (then heap abort). Causes: (1) `ncInt` neighbor fields were read through a `double` buffer and did not convert; (2) cell rings were rebuilt by `atan2` sort, which can self-intersect. Unpack now reads `Vneighs` / `VertexCellNeighbors` as `int`, walks the 3-valent mesh around each cell, and reverses a ring if signed area \(A<0\) (same \(A\) as `computeGeometryCPU`). N=64 CPU smoke: load \(q\approx4.22\), eq \(q\approx3.98\), exit 0. Resubmitted 57 `vertexTauAlpha` jobs on existing N=4096 scratch fluids (no fluid rerun). Cores gitignored; SLURM logs under `local/slurm-logs/`; `ulimit -c 0` in sbatch.
- How: Rebuild `vertexTauAlphaTest.out` (this host: `build-vertex-cuda129b`). Jobs use that binary; unpack source is not committed yet (submit echoed git HEAD `2ee03b99`). Other clones need the source then a rebuild before new jobs. `unset CELLGPU_SCRATCH`; `local/submit/submitVertexTauAlpha.sh` → SLURM `21405049`–`21405105` (57 jobs). Smoke: `CELLGPU_SCRATCH=/tmp/cellgpu_vertex_winding_smoke` then fluid `-n 64 -p 3.8 -v 0.1 -i 200 -t 200 -g -1` and tau `-n 64 -p 3.8 -v 0.05 -r 0 -t 1 -e 0.01 -g -1`.

## 2026-08-21 — Vertex-model tau_alpha scouting (Delta)

- Commit: `959a6ccfe`
- What: NVT restart NetCDF (`nvtAVMRestartDatabase`: vertices, topology, velocities, NH bath) and vertex-only trajectories (`vertexTrajectoryDatabase`). Drivers: high-T fluids `monodisperseVertex_equilibrium.cpp`; scouting `vertexTauAlphaTest.cpp` (15 log-spaced traj, SAC after 10 `tau_block`, `sigma_xy` every 100 dt). State points in `glassyDynamicsProject/vertexTauAlphaStatePoints.md` from Voronoi SISF tables. Data under `$CELLGPU_SCRATCH/N$N/vertex/` (default `/scratch/bbtm/cli6/glassyDynamics/data`).
- How: Rebuild host CMake targets `monodisperseVertex_equilibrium.out` and `vertexTauAlphaTest.out`. Fluids: `local/submit/submitVertexFluids.sh`. Tau jobs after fluids: `local/submit/submitVertexFluidsThenTau.sh`. `N=4096`, CPU, `dt=0.01`, `tau_block=max(tauAlpha,1000)`, `idx=0`. Smoke: `CELLGPU_SCRATCH=/tmp/cellGPU_smoke` with `-n 64`.

## 2026-08-21 — 2D Voronoi τ_α CSV tables

- Commit: `baae0c63c`
- What: Parsed 2D Voronoi `p0`, `T`, `tauAlpha` from `generalPicGlass/analyse/data_subArrhenius_2026_06_05.nb` and merged extra points from `Research/updates/2026_06_06/additionalTauAlpha_2026_06_06.csv`. Long-form and per-`p0` CSVs under `glassyDynamicsProject/analyseScripts/TauAlphaTable/data_2DVoronoi/`.
- How: `python3 glassyDynamicsProject/analyseScripts/TauAlphaTable/write_tauAlpha_2DVoronoi_tables.py`. Columns: `p0` (target shape index), `T` (reduced kT), `tauAlpha` (SISF 1/e), `source`. Combined: 71 notebook + 29 additional = 100 rows; extra `p0=3.815` only in the 2026-06-06 CSV.

## 2026-08-19 — Vertex σ_xy U-shape FD scan (random init)

- Commit: uncommitted (on `bd605d4a`)
- What: Random-init scan of `getSigmaXY()` vs central FD of \(E\), \(N=4096\), \(p_0=3.7,3.8,3.9\), 100 snapshots, \(\delta\gamma=10^{-2}\)–\(10^{-8}\). Mean \(|\Delta\sigma|\) is \(O((\delta\gamma)^2)\) then \(O(1/\delta\gamma)\). Note: `researchUpdates/chengling/weeklyUpdates/Chengling_2026_08_19.md`.
- How: `vertexSigmaXY_pScan.out -g -1 -n 4096 -c 100` from the repo root (writes `localTest/data/vertexSigmaXY_pScan.csv`). Plot: `python3 localTest/plot_vertexSigmaXY_pScan.py`.

## 2026-08-19 — Per-cell ∂E_i/∂γ for the vertex model

- Commit: uncommitted
- What: `VertexQuadraticEnergy::getdEdgamma(vector<double>&)` returns per-cell \(\partial E_i/\partial\gamma = -\sum F_x y_{\mathrm{rel}}\) over that cell's force sets (no \(1/A\) factor). FD test in `localTest/vertexSigmaXY.cpp`.
- How: After `git pull`, rebuild. `vertexSigmaXY.out -g -1 -n 4096 -c 1 -i 10000 -v 0.001`. Nose–Hoover NVT on vertices, \(T=0.001\), \(10^4\) steps, \(dt=0.01\). Mean abs/rel error over 4096 cells: \(|\Delta|\) is \(O(\delta\gamma^2)\) from \(10^{-2}\) to \(10^{-5}\) (decade ratio 100); mean rel. floors at \(\sim 9\times10^{-9}\) near \(\delta\gamma=10^{-5}\)–\(10^{-6}\). \(E\sim 10\).

## 2026-08-18 — Vertex-model analytic σ_xy

- Commit: uncommitted (on `bd605d4a`)
- What: `VertexQuadraticEnergy::getSigmaXY()` as \(\sigma_{xy}=(1/A)\partial E/\partial\gamma\) for simple shear. Uses existing force sets (`dEdv = -\partial E/\partial v`) times \(\partial v/\partial\gamma=(y_{\mathrm{rel}},0)\). FD test: `localTest/vertexSigmaXY.cpp` scans \(\delta\gamma=10^{-2}\ldots10^{-6}\).
- How: After `git pull`, rebuild. `vertexSigmaXY.out -g -1 -n 4096 -c 100 -i 10000 -v 0.1`. BD on vertices at \(T=0.1\), \(10^4\) steps, \(dt=0.01\). Mean \(E\) 542 vs \(5.2\times10^3\) random. Mean rel. error still \(O(\delta\gamma^2)\) then \(O(1/\delta\gamma)\); floor \(1.6\times10^{-8}\) at \(\delta\gamma=10^{-5}\) (random floor \(2.8\times10^{-8}\)). Mean \(|\Delta\sigma|\) floor improved ~10× with \(E\).

## 2026-08-18 — Mathematica notebooks on origin, not on simulation clones


- Commit: `010f06401`
- What: Added `.cursor/rules/mathematica-notebooks.mdc` and `simulation-clone.sparse-checkout`. `*.nb` stay in git for this analysis clone and GitHub; cluster clones skip them via sparse-checkout. Pushed untracked SE-relation notebooks (`additionalDataPointsForHelen.nb`, `nonGau2DVoro.nb`).
- How: Analysis machine: commit `*.nb` if under GitHub limits (skip >100 MB). Simulation clone: `git sparse-checkout init --no-cone` then `git sparse-checkout set --no-cone --stdin < simulation-clone.sparse-checkout`. Do not add `*.nb` to `.gitignore`.

## 2026-08-18 — Cage-relative mean fourth displacement (MFD)

- Commit: `eb0bbdce5`
- What: Added \(\langle|\Delta\mathbf{r}|^4\rangle\) in `dynamicalFeatures` (`MFDhelper`). 1-arg `computeCageRelativeMFD(currentPos)` uses initial-cage displacements (SISF path); 3-arg form uses unwrapped frame-to-frame displacements (MSD path). New drivers: `localTest/CRMFDProductionRuns.cpp`, `trueMFDProductionRuns.cpp`, `fs_productionRuns.cpp`, `fs_productionRuns_additional.cpp`.
- How: On each machine, `git pull` then rebuild. Run the new `.out`s like other production analyses (`-n`, `-p`, `-v` T, `-t`/`-m` wait time, `-r` record). Reduced units match existing MSD.

## 2026-08-18 — Cursor multi-machine workflow

- Commit: `78afd8aef`
- What: Added `.cursor/rules/multi-machine.mdc` so clones on SSH clusters/workstations share `src/` via git. Ignored `local/` for host-specific env and submit scripts.
- How: On each machine, `git pull`, rebuild, keep machine-only files under `local/`.
