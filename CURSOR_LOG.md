# Cursor / experiment log

Generated by Cursor. 2026-08-18. Newest entries at the top. Commit and push this file with shared source.

## 2026-08-21 — Vertex-model tau_alpha scouting (Delta)

- Commit: (uncommitted until Chengling asks)
- What: NVT restart NetCDF (`nvtAVMRestartDatabase`: vertices, topology, velocities, NH bath) and vertex-only trajectories (`vertexTrajectoryDatabase`). Drivers: high-T fluids `monodisperseVertex_equilibrium.cpp`; scouting `vertexTauAlphaTest.cpp` (15 log-spaced traj, SAC after 10 `tau_block`, `sigma_xy` every 100 dt). State points in `glassyDynamicsProject/vertexTauAlphaStatePoints.md` from Voronoi SISF tables. Data under `$CELLGPU_SCRATCH/N$N/vertex/` (default `/scratch/bbtm/cli6/glassyDynamics/data`).
- How: Rebuild host CMake targets `monodisperseVertex_equilibrium.out` and `vertexTauAlphaTest.out`. Fluids: `local/submit/submitVertexFluids.sh`. Tau jobs after fluids: `local/submit/submitVertexFluidsThenTau.sh`. `N=4096`, CPU, `dt=0.01`, `tau_block=max(tauAlpha,1000)`, `idx=0`. Smoke: `CELLGPU_SCRATCH=/tmp/cellGPU_smoke` with `-n 64`.

## 2026-08-21 — 2D Voronoi τ_α CSV tables

- Commit: `baae0c63c`
- What: Parsed 2D Voronoi `p0`, `T`, `tauAlpha` from `generalPicGlass/analyse/data_subArrhenius_2026_06_05.nb` and merged extra points from `Research/updates/2026_06_06/additionalTauAlpha_2026_06_06.csv`. Long-form and per-`p0` CSVs under `glassyDynamicsProject/analyseScripts/TauAlphaTable/data_2DVoronoi/`.
- How: `python3 glassyDynamicsProject/analyseScripts/TauAlphaTable/write_tauAlpha_2DVoronoi_tables.py`. Columns: `p0` (target shape index), `T` (reduced kT), `tauAlpha` (SISF 1/e), `source`. Combined: 71 notebook + 29 additional = 100 rows; extra `p0=3.815` only in the 2026-06-06 CSV.

## 2026-08-19 — Vertex σ_xy U-shape FD scan (random init)

- Commit: uncommitted (on `bd605d4a`)
- What: Random-init scan of `getSigmaXY()` vs central FD of \(E\), \(N=4096\), \(p_0=3.7,3.8,3.9\), 100 snapshots, \(\delta\gamma=10^{-2}\)–\(10^{-8}\). Mean \(|\Delta\sigma|\) is \(O((\delta\gamma)^2)\) then \(O(1/\delta\gamma)\). Note: `researchUpdates/chengling/weeklyUpdates/Chengling_2026_08_19.md`.
- How: `vertexSigmaXY_pScan.out -g -1 -n 4096 -c 100` from the repo root (writes `localTest/data/vertexSigmaXY_pScan.csv`). Plot: `python3 localTest/plot_vertexSigmaXY_pScan.py`.

## 2026-08-19 — Per-cell ∂E_i/∂γ for the vertex model

- Commit: uncommitted
- What: `VertexQuadraticEnergy::getdEdgamma(vector<double>&)` returns per-cell \(\partial E_i/\partial\gamma = -\sum F_x y_{\mathrm{rel}}\) over that cell's force sets (no \(1/A\) factor). FD test in `localTest/vertexSigmaXY.cpp`.
- How: After `git pull`, rebuild. `vertexSigmaXY.out -g -1 -n 4096 -c 1 -i 10000 -v 0.001`. Nose–Hoover NVT on vertices, \(T=0.001\), \(10^4\) steps, \(dt=0.01\). Mean abs/rel error over 4096 cells: \(|\Delta|\) is \(O(\delta\gamma^2)\) from \(10^{-2}\) to \(10^{-5}\) (decade ratio 100); mean rel. floors at \(\sim 9\times10^{-9}\) near \(\delta\gamma=10^{-5}\)–\(10^{-6}\). \(E\sim 10\).

## 2026-08-18 — Vertex-model analytic σ_xy

- Commit: uncommitted (on `bd605d4a`)
- What: `VertexQuadraticEnergy::getSigmaXY()` as \(\sigma_{xy}=(1/A)\partial E/\partial\gamma\) for simple shear. Uses existing force sets (`dEdv = -\partial E/\partial v`) times \(\partial v/\partial\gamma=(y_{\mathrm{rel}},0)\). FD test: `localTest/vertexSigmaXY.cpp` scans \(\delta\gamma=10^{-2}\ldots10^{-6}\).
- How: After `git pull`, rebuild. `vertexSigmaXY.out -g -1 -n 4096 -c 100 -i 10000 -v 0.1`. BD on vertices at \(T=0.1\), \(10^4\) steps, \(dt=0.01\). Mean \(E\) 542 vs \(5.2\times10^3\) random. Mean rel. error still \(O(\delta\gamma^2)\) then \(O(1/\delta\gamma)\); floor \(1.6\times10^{-8}\) at \(\delta\gamma=10^{-5}\) (random floor \(2.8\times10^{-8}\)). Mean \(|\Delta\sigma|\) floor improved ~10× with \(E\).

## 2026-08-18 — Mathematica notebooks on origin, not on simulation clones


- Commit: `010f06401`
- What: Added `.cursor/rules/mathematica-notebooks.mdc` and `simulation-clone.sparse-checkout`. `*.nb` stay in git for this analysis clone and GitHub; cluster clones skip them via sparse-checkout. Pushed untracked SE-relation notebooks (`additionalDataPointsForHelen.nb`, `nonGau2DVoro.nb`).
- How: Analysis machine: commit `*.nb` if under GitHub limits (skip >100 MB). Simulation clone: `git sparse-checkout init --no-cone` then `git sparse-checkout set --no-cone --stdin < simulation-clone.sparse-checkout`. Do not add `*.nb` to `.gitignore`.

## 2026-08-18 — Cage-relative mean fourth displacement (MFD)

- Commit: `eb0bbdce5`
- What: Added \(\langle|\Delta\mathbf{r}|^4\rangle\) in `dynamicalFeatures` (`MFDhelper`). 1-arg `computeCageRelativeMFD(currentPos)` uses initial-cage displacements (SISF path); 3-arg form uses unwrapped frame-to-frame displacements (MSD path). New drivers: `localTest/CRMFDProductionRuns.cpp`, `trueMFDProductionRuns.cpp`, `fs_productionRuns.cpp`, `fs_productionRuns_additional.cpp`.
- How: On each machine, `git pull` then rebuild. Run the new `.out`s like other production analyses (`-n`, `-p`, `-v` T, `-t`/`-m` wait time, `-r` record). Reduced units match existing MSD.

## 2026-08-18 — Cursor multi-machine workflow

- Commit: `78afd8aef`
- What: Added `.cursor/rules/multi-machine.mdc` so clones on SSH clusters/workstations share `src/` via git. Ignored `local/` for host-specific env and submit scripts.
- How: On each machine, `git pull`, rebuild, keep machine-only files under `local/`.
