# Cursor / experiment log

Generated by Cursor. 2026-08-18. Newest entries at the top. Commit and push this file with shared source.

## 2026-08-26 — Vertex τ_α scouting report and Voronoi comparison

- Commit: `5c6ad88bb`
- What: Combined batches 2–4 longest-crossed CRSISF \(1/e\) \(\tau_\alpha\). Recommended production \(T\) at \(\tau_k=10^{1+k/3}\) by interpolating \(\log\tau\) vs \(1/T\). Two-panel figure: \(\tau_\alpha\) vs \(T\) and Angell \(\tau_\alpha\) vs \(T_g/T\) (\(T_g\): \(\tau_\alpha=10^4\)), vertex vs Voronoi.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/plot_vertex_voronoi_comparison.py`. Report: `glassyDynamicsProject/analyseScripts/vertexTauAlpha/vertexModel_tauAlpha_report.md`. Outputs in `vertexModel_tauAlpha_final/`.

## 2026-08-26 — Vertex τ_α batch 4 CRSISF vs target

- Commit: `2ee03b998` (submit HEAD)
- What: Production for the 12 retargeted \(T\) all COMPLETED. CRSISF \(q=6.80\); per-curve \(\tau_\alpha\) is the \(1/e\) crossing. Plots vs \(T\) per \(p_0\): red short \(t_w\) to blue long \(t_w\), black = batch-4 target. 180 curves, 168 crossed \(1/e\).
- How: Tau jobs `21471973`–`21471984`. FS: `local/submit/submitVertexTauAlphaFsBatch4.sh` → `21481736`–`21481747`. Then `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha_batch4.py` → `vertexModel_tauAlpha_batch4/vertexModel_tauAlpha_batch4_vs_T_p*.png`.

## 2026-08-26 — Vertex τ_α fourth-batch T retarget

- Commit: `2ee03b998` (submit HEAD)
- What: Longest-crossed \(t_w\) \(1/e\) \(\tau_\alpha\) from batches 2–3 as the measured curve. Rerun targets still off by \(\ge 1.5\times\) (12 jobs). New \(T\) from walking \(\log\tau_{\mathrm{meas}}\) vs \(1/T\).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/propose_vertex_tau_alpha_batch4.py`. `local/submit/submitVertexTauAlphaBatch4.sh` → SLURM `21471973`–`21471984`. After production: `submitVertexTauAlphaFsBatch4.sh` then `measure_and_plot_tau_alpha_batch4.py`.

## 2026-08-26 — Vertex τ_α batch 3 CRSISF vs target

- Commit: `2ee03b998` (submit HEAD)
- What: Production for the 28 retargeted \(T\) all COMPLETED. CRSISF \(q=6.80\); per-curve \(\tau_\alpha\) is the \(1/e\) crossing. Plots vs \(T\) per \(p_0\): red short \(t_w\) to blue long \(t_w\), black = batch-3 target. 420 curves, 390 crossed \(1/e\).
- How: Tau jobs `21451592`–`21451598`, `21451600`–`21451620`. FS: `local/submit/submitVertexTauAlphaFsBatch3.sh` → `21471751`–`21471778`. Then `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha_batch3.py` → `vertexModel_tauAlpha_batch3/vertexModel_tauAlpha_batch3_vs_T_p*.png`.

## 2026-08-25 — Vertex τ_α third-batch T retarget

- Commit: `2ee03b998` (submit HEAD)
- What: Longest-\(t_w\) batch-2 \(1/e\) \(\tau_\alpha\) as the measured curve. Rerun targets \(\ge 1000\) and hot points off by \(\ge 1.5\times\) (28 jobs). New \(T\) from \(\log\tau_{\mathrm{meas}}\) vs \(1/T\).
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/propose_vertex_tau_alpha_batch3.py`. `local/submit/submitVertexTauAlphaBatch3.sh` → SLURM `21451592`–`21451598`, `21451600`–`21451620`.

## 2026-08-25 — Vertex τ_α batch 2 CRSISF vs target

- Commit: `2ee03b998` (submit HEAD)
- What: Second-batch production (60 jobs) all COMPLETED. CRSISF \(q=6.80\) on those trajectories; per-curve \(\tau_\alpha\) is the \(1/e\) crossing. Plots vs \(T\) per \(p_0\): red short \(t_w\) to blue long \(t_w\), black circles = batch-2 target \(\tau_{\alpha,k}=10^{1+k/3}\). 900 curves, 803 crossed \(1/e\).
- How: Tau jobs `21423560`–`21423619`. FS: `local/submit/submitVertexTauAlphaFsBatch2.sh` → `21450794`–`21450853`. Then `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha_batch2.py` → `vertexModel_tauAlpha_batch2/vertexModel_tauAlpha_batch2_vs_T_p*.png`.

## 2026-08-24 — Vertex τ_α second-batch T grid

- Commit: `2ee03b998` (submit HEAD; analysis uncommitted)
- What: Longest-\(t_w\) CRSISF \(1/e\) crossings give \(\tau_\alpha^{\mathrm{vertex}}\approx C(p_0)\tau_\alpha^{\mathrm{Voronoi}}\). Geometric-mean \(C\): 22.3, 28.6, 25.7, 47.4, 29.5, 42.8 for \(p_0=3.750\)–\(3.850\). Invented 10 \(T\) per \(p_0\) with \(\tau_{\alpha,k}=10^{1+k/3}\) (10 to \(10^4\)). Invert: \(\log\tau\) vs \(1/T\) in the Voronoi table; \(\log\tau\) vs \(\log T\) hotter than the table.
- How: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/propose_vertex_tau_alpha_batch2.py`. Points in `vertexModel_tauAlpha_batch2/`. `local/submit/submitVertexTauAlphaBatch2.sh` → SLURM `21423560`–`21423619` (60 CPU jobs, same `vertexTauAlphaTest.out`, existing \(T=0.1\) fluids). `-t` is the vertex \(\tau_\alpha\) target so \(\tau_{\mathrm{block}}=\max(\tau_{\mathrm{est}},1000)\).

## 2026-08-24 — Vertex CRSISF and per-curve τ_α

- Commit: uncommitted
- What: Cage-relative SISF of AVM cell centroids from first-batch vertex trajectories. Per-curve τ_α is the 1/e crossing (linear interpolation in Δt between the two samples that straddle 1/e). Plots: τ_α vs T per p0, red short t_w to blue long t_w, Voronoi estimate in black.
- How: Rebuild `vertexTauAlphaFs.out` (`build-vertex-cuda129b`). Default \(q=6.80\) (Voronoi `tauAlphaTableAnalyse`). FS on `$CELLGPU_SCRATCH/N4096/vertex/tauAlphaData/` (`k6.800` in the filename). First batch: SLURM `21413903`–`21413959` at \(q=2\pi\). Recompute: `21423359`–`21423415`. Plots: `python3 glassyDynamicsProject/analyseScripts/vertexTauAlpha/measure_and_plot_tau_alpha.py` and `plot_fs_grid_p3.800.py` → `vertexModel_tauAlpha_test/` (uses \(q=6.80\) files only).

## 2026-08-24 — AVM restart winding / NetCDF int read

- Commit: uncommitted
- What: Restart load of vertex fluids was producing NaN \(q\) (then heap abort). Causes: (1) `ncInt` neighbor fields were read through a `double` buffer and did not convert; (2) cell rings were rebuilt by `atan2` sort, which can self-intersect. Unpack now reads `Vneighs` / `VertexCellNeighbors` as `int`, walks the 3-valent mesh around each cell, and reverses a ring if signed area \(A<0\) (same \(A\) as `computeGeometryCPU`). N=64 CPU smoke: load \(q\approx4.22\), eq \(q\approx3.98\), exit 0. Resubmitted 57 `vertexTauAlpha` jobs on existing N=4096 scratch fluids (no fluid rerun). Cores gitignored; SLURM logs under `local/slurm-logs/`; `ulimit -c 0` in sbatch.
- How: Rebuild `vertexTauAlphaTest.out` (this host: `build-vertex-cuda129b`). Jobs use that binary; unpack source is not committed yet (submit echoed git HEAD `2ee03b99`). Other clones need the source then a rebuild before new jobs. `unset CELLGPU_SCRATCH`; `local/submit/submitVertexTauAlpha.sh` → SLURM `21405049`–`21405105` (57 jobs). Smoke: `CELLGPU_SCRATCH=/tmp/cellgpu_vertex_winding_smoke` then fluid `-n 64 -p 3.8 -v 0.1 -i 200 -t 200 -g -1` and tau `-n 64 -p 3.8 -v 0.05 -r 0 -t 1 -e 0.01 -g -1`.

## 2026-08-21 — Vertex-model tau_alpha scouting (Delta)

- Commit: `959a6ccfe`
- What: NVT restart NetCDF (`nvtAVMRestartDatabase`: vertices, topology, velocities, NH bath) and vertex-only trajectories (`vertexTrajectoryDatabase`). Drivers: high-T fluids `monodisperseVertex_equilibrium.cpp`; scouting `vertexTauAlphaTest.cpp` (15 log-spaced traj, SAC after 10 `tau_block`, `sigma_xy` every 100 dt). State points in `glassyDynamicsProject/vertexTauAlphaStatePoints.md` from Voronoi SISF tables. Data under `$CELLGPU_SCRATCH/N$N/vertex/` (default `/scratch/bbtm/cli6/glassyDynamics/data`).
- How: Rebuild host CMake targets `monodisperseVertex_equilibrium.out` and `vertexTauAlphaTest.out`. Fluids: `local/submit/submitVertexFluids.sh`. Tau jobs after fluids: `local/submit/submitVertexFluidsThenTau.sh`. `N=4096`, CPU, `dt=0.01`, `tau_block=max(tauAlpha,1000)`, `idx=0`. Smoke: `CELLGPU_SCRATCH=/tmp/cellGPU_smoke` with `-n 64`.

## 2026-08-21 — 2D Voronoi τ_α CSV tables

- Commit: `baae0c63c`
- What: Parsed 2D Voronoi `p0`, `T`, `tauAlpha` from `generalPicGlass/analyse/data_subArrhenius_2026_06_05.nb` and merged extra points from `Research/updates/2026_06_06/additionalTauAlpha_2026_06_06.csv`. Long-form and per-`p0` CSVs under `glassyDynamicsProject/analyseScripts/TauAlphaTable/data_2DVoronoi/`.
- How: `python3 glassyDynamicsProject/analyseScripts/TauAlphaTable/write_tauAlpha_2DVoronoi_tables.py`. Columns: `p0` (target shape index), `T` (reduced kT), `tauAlpha` (SISF 1/e), `source`. Combined: 71 notebook + 29 additional = 100 rows; extra `p0=3.815` only in the 2026-06-06 CSV.

## 2026-08-19 — Vertex σ_xy U-shape FD scan (random init)

- Commit: uncommitted (on `bd605d4a`)
- What: Random-init scan of `getSigmaXY()` vs central FD of \(E\), \(N=4096\), \(p_0=3.7,3.8,3.9\), 100 snapshots, \(\delta\gamma=10^{-2}\)–\(10^{-8}\). Mean \(|\Delta\sigma|\) is \(O((\delta\gamma)^2)\) then \(O(1/\delta\gamma)\). Note: `researchUpdates/chengling/weeklyUpdates/Chengling_2026_08_19.md`.
- How: `vertexSigmaXY_pScan.out -g -1 -n 4096 -c 100` from the repo root (writes `localTest/data/vertexSigmaXY_pScan.csv`). Plot: `python3 localTest/plot_vertexSigmaXY_pScan.py`.

## 2026-08-19 — Per-cell ∂E_i/∂γ for the vertex model

- Commit: uncommitted
- What: `VertexQuadraticEnergy::getdEdgamma(vector<double>&)` returns per-cell \(\partial E_i/\partial\gamma = -\sum F_x y_{\mathrm{rel}}\) over that cell's force sets (no \(1/A\) factor). FD test in `localTest/vertexSigmaXY.cpp`.
- How: After `git pull`, rebuild. `vertexSigmaXY.out -g -1 -n 4096 -c 1 -i 10000 -v 0.001`. Nose–Hoover NVT on vertices, \(T=0.001\), \(10^4\) steps, \(dt=0.01\). Mean abs/rel error over 4096 cells: \(|\Delta|\) is \(O(\delta\gamma^2)\) from \(10^{-2}\) to \(10^{-5}\) (decade ratio 100); mean rel. floors at \(\sim 9\times10^{-9}\) near \(\delta\gamma=10^{-5}\)–\(10^{-6}\). \(E\sim 10\).

## 2026-08-18 — Vertex-model analytic σ_xy

- Commit: uncommitted (on `bd605d4a`)
- What: `VertexQuadraticEnergy::getSigmaXY()` as \(\sigma_{xy}=(1/A)\partial E/\partial\gamma\) for simple shear. Uses existing force sets (`dEdv = -\partial E/\partial v`) times \(\partial v/\partial\gamma=(y_{\mathrm{rel}},0)\). FD test: `localTest/vertexSigmaXY.cpp` scans \(\delta\gamma=10^{-2}\ldots10^{-6}\).
- How: After `git pull`, rebuild. `vertexSigmaXY.out -g -1 -n 4096 -c 100 -i 10000 -v 0.1`. BD on vertices at \(T=0.1\), \(10^4\) steps, \(dt=0.01\). Mean \(E\) 542 vs \(5.2\times10^3\) random. Mean rel. error still \(O(\delta\gamma^2)\) then \(O(1/\delta\gamma)\); floor \(1.6\times10^{-8}\) at \(\delta\gamma=10^{-5}\) (random floor \(2.8\times10^{-8}\)). Mean \(|\Delta\sigma|\) floor improved ~10× with \(E\).

## 2026-08-18 — Mathematica notebooks on origin, not on simulation clones


- Commit: `010f06401`
- What: Added `.cursor/rules/mathematica-notebooks.mdc` and `simulation-clone.sparse-checkout`. `*.nb` stay in git for this analysis clone and GitHub; cluster clones skip them via sparse-checkout. Pushed untracked SE-relation notebooks (`additionalDataPointsForHelen.nb`, `nonGau2DVoro.nb`).
- How: Analysis machine: commit `*.nb` if under GitHub limits (skip >100 MB). Simulation clone: `git sparse-checkout init --no-cone` then `git sparse-checkout set --no-cone --stdin < simulation-clone.sparse-checkout`. Do not add `*.nb` to `.gitignore`.

## 2026-08-18 — Cage-relative mean fourth displacement (MFD)

- Commit: `eb0bbdce5`
- What: Added \(\langle|\Delta\mathbf{r}|^4\rangle\) in `dynamicalFeatures` (`MFDhelper`). 1-arg `computeCageRelativeMFD(currentPos)` uses initial-cage displacements (SISF path); 3-arg form uses unwrapped frame-to-frame displacements (MSD path). New drivers: `localTest/CRMFDProductionRuns.cpp`, `trueMFDProductionRuns.cpp`, `fs_productionRuns.cpp`, `fs_productionRuns_additional.cpp`.
- How: On each machine, `git pull` then rebuild. Run the new `.out`s like other production analyses (`-n`, `-p`, `-v` T, `-t`/`-m` wait time, `-r` record). Reduced units match existing MSD.

## 2026-08-18 — Cursor multi-machine workflow

- Commit: `78afd8aef`
- What: Added `.cursor/rules/multi-machine.mdc` so clones on SSH clusters/workstations share `src/` via git. Ignored `local/` for host-specific env and submit scripts.
- How: On each machine, `git pull`, rebuild, keep machine-only files under `local/`.
